# Superseded (2026-09-12)

The 2026-07 profiling suite (`scripts/`), its one-off scripts (`scratch/`), pinned requirements, experiment
logs (`exp_00*.md`) and the Overleaf report and presentation (`report_overleaf_2026-07/`). Kept for
reference only: two of the five experiments measured the wrong voxels or the wrong units
(see `../AUDIT_2026-09-12.md`), and the new pass under `../profiling/` replaces all of
them. Nothing here is run or maintained.
